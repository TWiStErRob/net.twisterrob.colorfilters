apply plugin: 'net.twisterrob.android-library'

dependencies {

	compile project(':feature:base')
	compile project(':feature:keyboard')

	compile "com.android.support:palette-v7:${VERSION_SUPPORT}"
}
