plugins {
	id("net.twisterrob.colorfilters.component.conventions")
}

android {
	namespace = "net.twisterrob.colorfilters.base"
}

dependencies {
	implementation(projects.feature.keyboard.contract)

	api(libs.androidx.annotation)
	api(libs.androidx.appcompat)
	api(libs.androidx.core.ktx)
	api(libs.androidx.drawerlayout)
	api(libs.androidx.fragment.ktx)
	debugImplementation(libs.androidx.test.fragment)
	api(libs.androidx.preference.ktx)

	testImplementation(libs.jmh)
	kaptTest(libs.jmh.processor)
}
