<?xml version="1.0" encoding="UTF-8"?>
<lint>
	<issue id="UnusedAttribute">
		<ignore regexp="\b(autofillHints)\b">
			<!-- used attribute for higher APIs only -->
		</ignore>
	</issue>
	<issue id="LintBaseline" severity="ignore">
		<!-- "# errors were filtered out because it is listed in the baseline file"
		     Hide these messages, baseline files only contain issues that are not suppressable otherwise. -->
	</issue>
	<issue id="RtlHardcoded" severity="ignore" />
	<issue id="GradleDependency" severity="ignore">
		<!-- Keep the build stable -->
	</issue>
	<issue id="SyntheticAccessor" severity="ignore">
		<!-- Don't want to deal with it yet -->
	</issue>
	<issue id="ImplicitSamInstance" severity="ignore">
		<!-- Don't want to deal with it yet -->
	</issue>

	<issue id="UnusedResources">
		<!-- Not used, but can't do anything about it :) -->
		<ignore regexp="R\.bool\.in_prod" />
		<ignore regexp="R\.bool\.in_test" />
		<!-- REPORT "The resource `R.font.` appears to be unused"
		     only comes up with checkDependencies = true -->
		<ignore regexp="R\.font\." />

		<!-- These are all used by :app's KeyboardView. -->
		<ignore path="**/res/drawable/keyboard_key.xml" />
		<ignore path="**/res/drawable-*/keyboard_key_*.9.png" />
	</issue>

	<issue id="IconMissingDensityFolder">
		<!-- REPORT Baseline uses absolute paths so have to ignore here -->
		<ignore regexp="feature.keyboard.src.main.res" />
	</issue>

	<issue id="IconColors">
		<!-- menu/activity_color_filter.xml/action_logo uses ic_launcher.png as an icon,
		     so it'll be classified as an ActionBar icon.
		     See IconDetector.MenuFinder writing to actionBarIcons and isActionBarIcon() reading that. -->
		<ignore path="**/res/drawable-*/ic_launcher.png" />
	</issue>

	<issue id="DuplicateStrings">
		<!-- REPORT "Duplicate string value G:, used in cf_matrix_G and cf_matrix_G"
		     duplicate check doesn't take HTML contents into account -->
		<ignore regexp="used in `(cf_matrix_.*)` and `\1`" />

		<!-- Ignore resources from exploded AARs in Gradle caches, don't care about duplications in abc_* -->
		<!-- TODEL https://issuetracker.google.com/issues/113673427 -->
		<ignore path="**/caches/transforms-*/files-*/*" />
	</issue>

	<issue id="InvalidPackage">

		<!--
			gradle/caches/modules-2/files-2.1/net.bytebuddy/byte-buddy-agent/1.12.9/f58bf71d15e827fa47e9b37e63156b333e3389d3/byte-buddy-agent-1.12.9.jar
	        Invalid package reference in net.bytebuddy:byte-buddy-agent; not included in Android: `java.lang.instrument`. Referenced from `net.bytebuddy.agent.ByteBuddyAgent`.
		-->
		<ignore regexp="^Invalid package reference in net.bytebuddy:byte-buddy-agent; not included in Android: `java\.lang\.instrument`\. Referenced from `net\.bytebuddy\.agent\.ByteBuddyAgent`\.$" />

		<!--
			gradle/caches/modules-2/files-2.1/net.bytebuddy/byte-buddy-agent/1.12.9/f58bf71d15e827fa47e9b37e63156b333e3389d3/byte-buddy-agent-1.12.9.jar
			Invalid package reference in net.bytebuddy:byte-buddy-agent; not included in Android: `java.lang.management`. Referenced from `net.bytebuddy.agent.ByteBuddyAgent.ProcessProvider.ForCurrentVm.ForLegacyVm`.
		-->
		<ignore regexp="^Invalid package reference in net.bytebuddy:byte-buddy-agent; not included in Android: `java\.lang\.management`\. Referenced from `net\.bytebuddy\.agent\.ByteBuddyAgent\.ProcessProvider\.ForCurrentVm\.ForLegacyVm`\.$" />

		<!--
			gradle\caches\modules-2\files-2.1\org.jetbrains.kotlinx\kotlinx-coroutines-core-jvm\1.5.2\f4cc07a50437659e0043e7da762809a46932b6a0\kotlinx-coroutines-core-jvm-1.5.2.jar
			Invalid package reference in org.jetbrains.kotlinx:kotlinx-coroutines-core-jvm; not included in Android: java.lang.instrument. Referenced from kotlinx.coroutines.debug.AgentPremain.
		-->
		<ignore regexp="^Invalid package reference in org.jetbrains.kotlinx:kotlinx-coroutines-core-jvm; not included in Android: `java\.lang\.instrument`\. Referenced from `kotlinx\.coroutines\.debug\.AgentPremain`\.$" />

	</issue>

</lint>
